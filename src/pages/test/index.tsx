import { Button } from '@/components'

const TestPage = () => {
  return (
    <div>
      <Button size="tiny" color="reverse">
        로그인
      </Button>
      <Button size="tiny">회원가입</Button>
    </div>
  )
}

export default TestPage
